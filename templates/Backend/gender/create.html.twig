{% extends 'base.html.twig' %}

{% block title %}
    Création d'un genre
     {{ parent() }}
{% endblock %}

{% block body %}
    <h1 class="container mt-2"> Création d'un genre</h1>
    {{form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Créer</button>

    {{form_end(form) }}
{% endblock %}
