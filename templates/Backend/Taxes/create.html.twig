{% extends 'base.html.twig' %}

{% block title %}
    Nouvelle taxe
     {{ parent() }}
{% endblock %}

{% block body %}
    <h1 class="container mt-2">Nouvelle taxe</h1>
    {{form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">Créer</button>

    {{form_end(form) }}
{% endblock %}